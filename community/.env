# Django Production Environment Variables
# Configure these values for production deployment

# Debug mode (set to False for production)
DEBUG=True

# Django secret key (generate a new one for production)
# You can generate one using: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=django-insecure-^y)fu5%q93p*n3rm_$ara%g&-sgr!utop!+b)(bwp3_u@_ky5-

# Allowed hosts (comma-separated list of domain names)
ALLOWED_HOSTS=localhost,127.0.0.1

# Redis URL for channel layers (required for production)
REDIS_URL=redis://localhost:6379/0
